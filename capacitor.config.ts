import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'elevator-measurement-calculator',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
