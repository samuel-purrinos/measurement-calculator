import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.uichesoh.agrelo-elevators',
  appName: 'algrelo-elevators',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
